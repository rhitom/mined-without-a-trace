{
  "buildCommand": "cd project-final && npm install --ignore-scripts && npx next build",
  "outputDirectory": "project-final/.next",
  "installCommand": "cd project-final && npm install --ignore-scripts",
  "framework": "nextjs"
}
