{
  "rootDirectory": "project-final",
  "buildCommand": "npm install --ignore-scripts && npx next build",
  "outputDirectory": ".next",
  "installCommand": "npm install --ignore-scripts",
  "framework": "nextjs"
}
